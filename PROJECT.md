# Anywhere Project Guidelines

Legend (from RFC2119): !=MUST, ~=SHOULD, ≉=SHOULD NOT, ⊗=MUST NOT, ?=MAY.

**deft_version**: 0.15.0

Only specify items here that **override or extend** the deft defaults.

**Reference**: `directive/main.md` | `directive/languages/typescript.md`

## Project Configuration

**Tech Stack**: Library using TypeScript — tsup (bundler) + Vitest (tests) + serialport (serial transport)

**Target**: Cross-platform — Browser, Node.js, and native runtimes via Capacitor / Electron / Tauri

## Strategy

Use [Interview](directive/strategies/interview.md) for this project.

## Workflow

```bash
task check          # Pre-commit gate (lint + typecheck + test + coverage)
task test:coverage  # Coverage target (≥85%)
task build          # Build library (tsup → dist/)
task clean          # Clean artifacts
```

## Standards

**Quality:**
- ! Run `task check` before every commit
- ! Achieve ≥85% coverage overall + per-module
- ! New source files MUST include corresponding test files in the same PR

## Project-Specific Rules

(Add your custom rules here)

## Branching

Allow direct commits to master: true

---

**Generated by**: deft-setup skill
**Date**: 2026-04-13
